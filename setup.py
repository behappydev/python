from setuptools import setup, find_packages

setup(
    name='shop',
    version='0.1.0',
    description='Paquete para gestión de clientes y catálogo con panel de administración',
    packages=find_packages(),
    python_requires='>=3.6',
)